// CSRF functionality has been removed for simplified authentication
// These are stub functions to prevent import errors

// Function to fetch CSRF token (disabled)
export const fetchCsrfToken = async () => {
  return null;
};

// Get CSRF token (disabled)
export const getCsrfToken = async () => {
  return null;
};

// Clear CSRF token (disabled)
export const clearCsrfToken = () => {
  // No-op
};